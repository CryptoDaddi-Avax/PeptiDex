import RedesignLayout from '@/components/redesign/RedesignLayout';

export default function VendorsGroupLayout({ children }: { children: React.ReactNode }) {
  return <RedesignLayout>{children}</RedesignLayout>;
}
